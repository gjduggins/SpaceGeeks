@page
@model IndexModel
@{
    ViewData["Title"] = "Solar System Planets";
}

<h1 class="visually-hidden">Solar System Planets</h1>

<div class="planet-grid">
    @foreach (var planet in Model.Planets)
    {
        <partial name="_PlanetCard" model="planet" />
    }
</div>
